# MoneyTrail TODOS

Generated by /autoplan on 2026-05-01.

## Critical — Must fix before Phase 1 ships

- [ ] Add LLM output validation layer (Pydantic models) for all extraction outputs before any DB write
- [ ] Add `needs_review INTEGER` to `asset_mentions` schema (data loss fix)
- [ ] Add `raw_llm_response TEXT` to `asset_mentions` for auditability
- [ ] Create `scripts/init_db.py` with all CREATE TABLE statements (idempotent, WAL mode, busy_timeout)
- [ ] Create `schema.sql` defining all 16 tables
- [ ] Create `SETUP.md` with prerequisites, install steps, database init, and smoke test
- [ ] Document `config/telegram.yaml` schema (bot token, chat ID, rate limits)
- [ ] Document `config/sources.yaml` schema with example for local Markdown drop
- [ ] Add example `agents/source_collector.md` showing complete OpenClaw agent format

## High — Required for correctness

- [ ] Add `price_history` table: `(asset, date, close_price, volume, source, fetched_at)`
- [ ] Add `scripts/refresh_prices.py` to daily workflow (step 12)
- [ ] Add `real_positions` table for Portfolio Heat Engine current-holdings reference
- [ ] Add `resolution_queue` table for entity resolver ambiguity handling
- [ ] Add `pipeline_runs` table for stage tracking and restartability
- [ ] Specify SQLite WAL mode + `busy_timeout=5000` in init_db.py
- [ ] Define normalization spec for all 5 composite scores in `config/scoring.yaml`
- [ ] Add all 5 remaining config schemas: `scoring.yaml`, `research_rules.yaml`, `thesis_budget.yaml`, `assets.yaml`, `research_rules.yaml`
- [ ] Add Telegram bot token to secrets strategy; create `secrets/.env.example`
- [ ] Add few-shot examples (5) to `prompts/extraction_prompt.md`
- [ ] Replace continuous confidence float with 3-tier categorical: `high|medium|low`
- [ ] Define per-board data freshness timestamps for here.now dashboard
- [ ] Define dashboard information hierarchy (landing view = portfolio heat + today's signals + approval queue)
- [ ] Define empty/error states for each of 6 dashboard boards
- [ ] Add logs/ strategy: naming convention, format, per-step status
- [ ] Add macOS launchd plist for daily pipeline automation
- [ ] Document all threshold values in `scoring.yaml` (signal_threshold, research_threshold, etc.)
- [ ] Add `asset_thesis_scores` junction table (replaces `asset_theses` primary key on asset)
- [ ] Add version history columns to `asset_theses`: `version INTEGER`, `superseded_by TEXT`

## Medium — Quality improvements

- [ ] Add conviction override fast-track path to Decision Ladder (Section 12)
- [ ] Add N+1 query fix: JOIN-based signal scorer replacing per-asset loop
- [ ] Add `CREATE INDEX` statements to init_db.py: `asset_mentions(symbol)`, `asset_mentions(content_id)`, `asset_signals(symbol)`, `venue_assets(symbol, venue)`
- [ ] Add migration framework: `migrations/001_initial.sql`, `schema_version` table
- [ ] Define allocation rule: asset counts toward primary thesis only (not both)
- [ ] Specify staleness threshold for Hyperliquid data in `config/tradeability.yaml`
- [ ] Add circuit breaker to `scripts/refresh_hyperliquid_universe.py`
- [ ] Add structured `invalidation_conditions` format (JSON array, not free text) to `asset_theses`
- [ ] Add venue column to `lilo_profiles` (breaks if same asset held on 2 venues)
- [ ] Define volatility_buffer as computed value (realized_30d_vol × multiplier) not hardcoded constant
- [ ] Add "Adding a New Source" runbook
- [ ] Add per-phase migration notes to Section 19 (additive vs destructive changes per phase)
- [ ] Specify here.now delivery mechanism (static HTML served locally vs web app vs Telegram-embedded)
- [ ] Add color convention spec to Section 15: Red/Amber/Green/Gray per status type
- [ ] Specify quantified triggers in LILO Board "Next Layer" column (price levels, not narrative)
- [ ] Add time basis label to Best Allocation Board return figures (YTD / since inception)
- [ ] Add "mandatory decisions" section to Weekly Memo template
- [ ] Define Telegram digest priority ordering (approval packets first, portfolio heat second)

## Future / Post-validation

- [ ] Multi-venue abstraction (Hyperliquid + IBKR/Coinbase for equities and ETFs)
- [ ] Third thesis: Capital Preservation / Dry Powder with explicit allocation target
- [ ] Competitive landscape analysis (Messari, Kaito AI, Token Terminal vs MoneyTrail)
- [ ] Scoring weight calibration mechanism after 90 days of decision data
- [ ] Source accountability: per-source accuracy metrics after 30+ decisions tracked
